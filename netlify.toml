# Despliegue de RITMO (Next.js) en Netlify.
# El plugin oficial detecta el App Router, las rutas de API y el proxy de sesión.
[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"
